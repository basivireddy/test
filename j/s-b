pipeline {
    agent any
    parameters {
    string(name: 'STATEMENT', defaultValue: 'test,terraform', description: 'What should I say?')
  }
  
    stages {
        stage('Process CSV') {
            steps {
                script {
                    // Define your comma-separated string
                    def csvString = params.STATEMENT
                    
                    // Split the string into a list
                    def values = csvString.split(',')
                    env.message = ""
                   // sh "ls -la"
                    sh "chmod +x s/s-b.sh"
                    
                    // Print each value
                    values.each { value ->
                        echo "Value: ${value}"
                        dir(value){
                            git branch: 'main' , url: "https://github.com/basivireddy/${value}.git"
                        }
                      //  sh "ls -la ${value}"
                      
                      sh "bash s/s-b.sh  ${value} > stale-branch-${value}.csv"
                      def files = findFiles(glob: "stale-branch-${value}.csv")
                      env.message = env.message + "${files[0].name} have stale branch : ${files[0].length} \n"
                    }
                }
            }
        }

        stage(notification){
        steps {
           archiveArtifacts artifacts: '*.csv', fingerprint: true
           echo "${env.message}"
        
        }
        }
    }
}
